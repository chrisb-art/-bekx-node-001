# BEKX X Publisher

Upload `api/x-publish.js` into the existing repository `api/` folder.

It publishes only records in `social_posts` whose status is `APPROVED`.
It uses the existing Vercel secret `X_ACCESS_TOKEN`.
